# RevenueCat API Keys
# Get these from: https://app.revenuecat.com/
# 
# For iOS: Go to your RevenueCat project → Settings → API Keys → iOS API Key
# For Android: Go to your RevenueCat project → Settings → API Keys → Android API Key
#
# Note: These are public API keys (safe to include in client apps)
# They are different from your secret API keys (server-side only)

#REVENUECAT_API_KEY_IOS=your_ios_api_key_here
#REVENUECAT_API_KEY_ANDROID=your_android_api_key_here

# Optional: Single key (if using same key for both platforms)
# REVENUECAT_API_KEY=your_api_key_here
